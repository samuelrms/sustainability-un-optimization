import React from "react";
import { ContainerError } from "./styled";

export const Error = () => {
  return (
    <ContainerError>
      <h1>
        Acreditamos que você esteja no local errado
        {/* , por gentileza clique no
        botão abaixo para retornar para pagina inicial */}
      </h1>
    </ContainerError>
  );
};
